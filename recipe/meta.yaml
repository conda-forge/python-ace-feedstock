{% set name = "python-ace" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ICAMS/python-ace/archive/29ba519d76eed9f6654d6633a9911336cb13da14.tar.gz
  sha256: b78a42e5678953596e03a5f0b2de828466add9e7b23c6267eab40f5bdd42e8f6
  patches:
    - setup.patch  # [osx]
    - pybind.patch
    - yaml.patch

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: true  # [win]

requirements:
  build:
    - cmake
    - make
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
  host:
    - python
    - pip
    - pybind11
  run:
    - python
    - ase
    - numpy
    - pandas
    - psutil
    - ruamel.yaml
    - libcxx  # [osx]

test:
  imports:
    - pyace
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/ICAMS/python-ace/
  summary: 'pacemaker - a tool for fitting of interatomic potentials in a general nonlinear Atomic Cluster Expansion (ACE) form'
  description: |
    pacemaker - a tool for fitting of interatomic potentials in a general
    nonlinear Atomic Cluster Expansion (ACE) form.
  license: ASL
  license_family: OTHER
  license_file: LICENSE.md
  doc_url: https://pacemaker.readthedocs.io
  dev_url: https://github.com/ICAMS/python-ace/

extra:
  recipe-maintainers:
    - srmnitc
    - jan-janssen
